import { describe, it, expect } from 'vitest';
// Simple test to ensure test runner works.
describe('Basic test', () => {
  it('should run tests correctly', () => {
    expect(true).toBe(true);
  });
});
